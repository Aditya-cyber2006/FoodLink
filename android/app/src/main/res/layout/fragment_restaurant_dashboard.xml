<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_light">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Post Surplus Food"
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="@color/secondary"
            android:layout_marginBottom="6dp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Create a listing and make it available instantly to nearby NGOs."
            android:textColor="@color/text_secondary"
            android:textSize="14sp"
            android:layout_marginBottom="16dp" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp"
            android:background="@drawable/card_bg"
            android:layout_marginBottom="24dp">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Add New Food Listing"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="@color/primary"
                android:layout_marginBottom="16dp" />

            <EditText
                android:id="@+id/food_name_input"
                android:layout_width="match_parent"
                android:layout_height="44dp"
                android:hint="Food Name (e.g., Pizza, Biryani)"
                android:paddingStart="12dp"
                android:paddingEnd="12dp"
                android:layout_marginBottom="12dp"
                android:background="@drawable/edit_text_bg"
                android:textColorHint="@color/text_secondary"
                android:textColor="@color/text_primary" />

            <EditText
                android:id="@+id/quantity_input"
                android:layout_width="match_parent"
                android:layout_height="44dp"
                android:hint="Quantity (e.g., 10)"
                android:inputType="number"
                android:paddingStart="12dp"
                android:paddingEnd="12dp"
                android:layout_marginBottom="12dp"
                android:background="@drawable/edit_text_bg"
                android:textColorHint="@color/text_secondary"
                android:textColor="@color/text_primary" />

            <EditText
                android:id="@+id/expiry_time_input"
                android:layout_width="match_parent"
                android:layout_height="44dp"
                android:hint="Pick up in (e.g., 2 hours)"
                android:paddingStart="12dp"
                android:paddingEnd="12dp"
                android:layout_marginBottom="12dp"
                android:background="@drawable/edit_text_bg"
                android:textColorHint="@color/text_secondary"
                android:textColor="@color/text_primary" />

            <EditText
                android:id="@+id/location_input"
                android:layout_width="match_parent"
                android:layout_height="44dp"
                android:hint="Location (e.g., Downtown)"
                android:paddingStart="12dp"
                android:paddingEnd="12dp"
                android:layout_marginBottom="16dp"
                android:background="@drawable/edit_text_bg"
                android:textColorHint="@color/text_secondary"
                android:textColor="@color/text_primary" />

            <Button
                android:id="@+id/add_listing_button"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:text="Add New Listing"
                android:textAllCaps="false"
                android:textSize="15sp"
                android:textStyle="bold"
                android:textColor="@color/white"
                android:backgroundTint="@color/primary" />

        </LinearLayout>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Your Active Listings"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@color/secondary"
            android:layout_marginBottom="12dp" />

        <LinearLayout
            android:id="@+id/restaurant_listings_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />

    </LinearLayout>

</ScrollView>
